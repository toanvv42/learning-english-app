services:
  pronunciation-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - HF_HOME=/models
      - PRONUNCIATION_DEVICE=cpu
    volumes:
      - hf_cache:/models

volumes:
  hf_cache:
